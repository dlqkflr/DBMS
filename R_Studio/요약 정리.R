<- 대입 연산자

print 프린트함수

c() vector 형성 (array와 비슷함)

str() 객체형태 분석 함수

length() 길이 찾기 함수

identical(a,b) a와 b가 같은지 확인하는 함수

mode() 데이터타입 분석함수

min/ max/ mean /median /sum 최소/최대/평균/중간값/합계

TRUE, FALSE = T,F   ! = not

nchar() 문자열 갯수 찾아주는 함수

substr("문자",시작위치,마지막위치) 문자안에서 위치에 따라찾아줌

strsplit('문자열', split="나눌 구분") 구분기호를 기준으로 문자 나눠줌

paste(sep="기준") = join이랑 같음 

toupper 대문자 변형

tolower 소문자 변형

factor(x, labels()범주, ordered()서열) 범주함수로 인식함

as.character() / as.numeric()  형변환 

3:7 range값처럼 3~7까지 만들어줌

seq(from=10, to=20, by=2) 10:2:2 의 값

append(vector_a, vector_b, 3) insert 함수와 같음 3번째에 넣음.

나머지 불린값, 인덱스, 덧셈, 뺄셈 곱셈은 파이썬과 동일

